import type { Metadata } from 'next'
import type { ReactNode } from 'react'

export const metadata: Metadata = {
  title: 'Update Design | Inscription',
}

export default function InscriptionLayout({ children }: { children: ReactNode }) {
  return children
}
