import type { Metadata } from 'next'
import type { ReactNode } from 'react'

export const metadata: Metadata = {
  title: 'Update Design | Connexion',
}

export default function ConnexionLayout({ children }: { children: ReactNode }) {
  return children
}
